<h1>{{pageName}}</h1>
<table class="t01" border="1">
    <thead>
        <tr>
            <th>Category</th>
            <th>Record Holder</th>
            <th>Record</th>
        </tr>
    </thead>
    <tbody>
        {{#if rows.length }}
            {{#each rows }}
                <tr>
                    <td>{{this.category}}</td>
                    <td>{{this.name}}</td>
                    <td>{{this.Record}}</td>
                </tr>
            {{/each}}
        {{else}}
            <tr><td colspan="3"><p class='lead'><em>No records were found.</em></p></td></tr>
        {{/if}}
    </tbody>
</table>
